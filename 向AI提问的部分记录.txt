
我现在有个问题，需要你给我解决思路： 我希望可以加一个“历史记录”的功能，点击后可以出现我之前所生成的智能知识图谱。并且保留之前已经点亮的标记。        我目前有些思路：智能知识图谱是根据生成的这种api的关键词（例如["SCUPI+", "包含", "Data Structure"]），将之转化为图像后输出的。那我就保留之前生成的api文件，需要看到历史时则调出api文件，再转化为图的形式，生成原先有的知识图谱。        关于“保留之前已经点亮的标记”，我认为需要修改api文件中储存的格式——目前是3个标识，例如“["SCUPI+", "包含", "Film Query Application"]”。 我希望再加一个——点亮状态：True / False，用布尔来表示该知识点是否已被掌握，节点是否已被点亮。 若已点亮，则第四个标识记为“True”。那么再次生成时节点的颜色就是绿色；否则即为红色。在最开始所有节点的点亮状态(即第四个标识)默认为False，例如：["SCUPI+", "包含", "Data Structure", "False"] ，在该节点被判断已被掌握后变为True       
你看看我的思路能否实现？有无需要优化的点？评价一下。



有几个问题：（1）我没有看到新的api_response文件，他应该单独出现在我的文件夹api_responses里。      （2）历史记录打开后有显示，但无法加载之前的历史图谱。    
我希望以这种方式实现：在生成知识图谱后保存他的api_response文件，在从历史记录中点击他时，其实就是重新调用它api_response文件中的内容，根据里面的标签再次生成图像以复现。  若节点的点亮状态变化，则每次变化后它的api_respons文件都会被更新并覆盖。   你分析一下我的思路实用性与可行性，给出建议。



我发现点击历史记录后生成的谱图和刚开始生成的样子不同，二者都是通过相同的api_response文件来生成的，所以这应该是点击历史记录生成谱图的前端代码与最开始的不一样，帮我改正。


点击历史记录后底部显示：”节点: 11 边: 10“这些图谱的节点数和边数是没问题的， 但图谱没重新生成出来啊！ 这是什么原因？应该重新调用原先文件中api_response文件里的内容，根据里面的标签再次生成图像以复现。


api_responses文件夹中没有生成复制的api文件，应该在第一版图谱生成后就立即将api_response文件复制到api_responses文件夹里。从历史记录中点击时，就重新调用它api_responses文件夹中的api_response文件，根据里面的标签再次生成图像。若节点的点亮状态变化，则每次变化后api_responses文件夹中的api_respons文件都会被更新并覆盖。 能明白的我的意思吗？


知识图谱生成实现流程如下：
一。文件上传处理
1. 通过/api/generate接口接收用户上传的文件和节点数量参数
2. 验证文件大小（限制50MB）
3. 生成唯一ID(topology_id)保存文件
二。文档处理
1. 调用process_document函数处理文件
2. 根据文件类型(PDF/Word/PPT等)解析提取文本内容
3. 文本过长时截取前8000字符
三。知识提取
1. 调用extract_knowledge_from_text函数
2. 使用DeepSeek API分析文本内容
3. 提取概念及其关系，形成三元组列表
四。构建树形结构
1. 构建节点和边的集合
2. 计算节点的层级和重要性
3. 根据设置的最大节点数进行过滤
五。保存到数据库
1. 将拓扑图、节点和边信息保存到数据库表
2. 保存API响应到文件系统
3. 更新全局变量和进度信息


继续改善：
1. 为了进一步增强用户身份验证，可以考虑在用户登录后将用户ID保存到localStorage。
2. 可以考虑在登录系统和主应用之间共享会话数据，确保用户身份的一致性。